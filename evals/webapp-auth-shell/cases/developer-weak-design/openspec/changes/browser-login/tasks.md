# Tasks

1. Implement the custom endpoint limiter.
2. Implement `LoginNonceService` with store serialization and expiry.
3. Decode the access token in `SessionService`.
4. Add tests and make them pass.
