# Browser login

Let a user begin login from a browser, confirm it in an existing chat integration,
and then receive an authenticated browser session. Repeated start attempts from
one address must be limited. Login requests expire and can be consumed only once.
