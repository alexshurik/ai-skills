# Review scope fixture

This directory is materialized by `build-fixture.sh`. The committed base contains
an unrelated oversized legacy module. The working tree then adds an untracked
transport module with a new boundary violation.
