# Approved Project Conventions

- Keep one-use validation bounds and algorithm values with their owning declaration.
- Keep imports at module scope unless a clean-process test reproduces a real cycle.
- Do not create dependency aliases or forwarding wrappers without a semantic
  contract or real reuse.
