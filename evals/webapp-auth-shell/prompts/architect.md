Use the `sk-architect` workflow from the supplied skills source to design the
feature described in `cases/architect-proposal.md`. Inspect the target repository
and its existing integrations. Do not implement code.
