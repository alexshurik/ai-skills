Use the `sk-developer` workflow from the supplied skills source. Review the approved
artifacts and implementation tasks in the target repository, then proceed as that
workflow requires. Do not assume that existing source patterns are approved.
